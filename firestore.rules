rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For local development, allow all reads and writes
    // WARNING: Do not deploy these rules to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
